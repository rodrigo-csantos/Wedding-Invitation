const path = require('path')

const pagehome = path.join(__dirname, '..', '..', '..', 'frontend', 'index.html')

const pageGiftList = path.join(__dirname, '..', '..', '..', 'frontend', 'gift-list.html')

module.exports = {
    pagehome,
    pageGiftList
}